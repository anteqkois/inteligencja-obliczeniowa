# from .rr_exits_short_with_stopsrc_2d import rr_exits_short_with_stopsrc_2d

# __all__ = [
# 	"rr_exits_short_with_stopsrc_2d",
# ]


# def solve_tsp(distance_matrix, params):
#     """
#     distance_matrix: np.ndarray (kwadratowa macierz odległości)
#     params: dict — słownik parametrów specyficznych dla algorytmu
#     Zwraca:
#         route: list[int] — kolejność odwiedzanych miast
#         cost: float — długość trasy
#         runtime: float — czas wykonania (sekundy)
#         meta: dict — opcjonalne dane np. liczba iteracji, historia, itp.
#     """
